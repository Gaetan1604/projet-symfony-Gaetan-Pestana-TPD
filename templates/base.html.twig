<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <title>{% block title %}Welcome!{% endblock %}</title>

        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
    </head>
    <body>
        {% include 'navbar.html.twig' %}

        <div class="container-fluid">
            <div class="row">
                <!-- Barre latérale -->
                <div class="col-md-3 col-lg-2 sidenavbar-container">
                    {% include 'sidenavbar.html.twig' %}
                </div>

                <!-- Contenu principal -->
                <div class="col-md-9 col-lg-10">
                    <main class="py-4">
                        {% block body %}
                        {% endblock %}
                    </main>
                </div>
            </div>
        </div>

        {% include 'footer.html.twig' %}
    </body>
</html>
